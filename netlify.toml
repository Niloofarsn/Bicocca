[build]
  command = "npm run build"
  publish = "_site"

[build.environment]
  NODE_VERSION = "20"

# Send the bare domain to the English homepage.
[[redirects]]
  from = "/"
  to = "/en/"
  status = 302
